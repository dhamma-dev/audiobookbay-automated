# Pinned for reproducible builds (v2). Bump deliberately; CI runs the test
# suite against exactly these versions before an image ever publishes.
flask==3.1.3
waitress==3.0.2
requests[socks]==2.34.2
beautifulsoup4==4.15.0
qbittorrent-api==2026.6.1
python-dotenv==1.2.2
transmission-rpc==7.0.11
deluge-web-client==2.0.3
google-genai==2.10.0
